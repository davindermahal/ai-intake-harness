# Copy to .env.local (gitignored — never commit it with real values) and fill in your Jira
# credentials. These three are the only vars lib/tracker/jira-common.sh's jira_common_load_env
# loads from .env / .env.local; everything else (TRACKER, TRACKER_PROJECT_KEY, TRACKER_APP_TAG,
# ...) is non-secret config and belongs in .ai/intake.config instead — see README.md "Quickstart".
#
# `install.sh` copies this file to .env.local for you and runs a connectivity check once it's
# filled in.

JIRA_SITE_URL=https://your-site.atlassian.net
JIRA_INTAKE_EMAIL=you@your-domain.com
JIRA_INTAKE_API_TOKEN=your-api-token-here
